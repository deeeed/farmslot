# Optional local-only gateway auth for remote/WSS development.
# Copy to .env.local-auth (gitignored) and set exactly one credential.
# Generate a token with: openssl rand -base64 32

# FARMSLOT_GATEWAY_TOKEN=
# FARMSLOT_GATEWAY_PASSWORD=
