/** Default-path touch targets (iOS HIG / Material ~44pt primary). RN-free for unit tests. */
export const touchTargets = {
  primaryMin: 44,
  primaryMinHeight: 44,
  primaryMinWidth: 44,
  secondaryMinHeight: 36,
} as const;
