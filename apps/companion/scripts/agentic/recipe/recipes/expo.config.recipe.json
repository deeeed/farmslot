{
  "schema_version": 1,
  "title": "Expo configuration smoke",
  "description": "Verifies the Expo project can run a Farmslot Recipe Protocol v1 artifact-producing configuration check.",
  "proofTargets": [
    {
      "id": "expo-config",
      "claim": "The Expo project can execute a Farmslot recipe runner and expose public config without secrets."
    }
  ],
  "validate": {
    "workflow": {
      "entry": "expo-config",
      "nodes": {
        "expo-config": {
          "action": "command",
          "description": "Read Expo public config through the project package manager.",
          "cmd": "yarn expo config --type public --json",
          "timeout_ms": 120000,
          "next": "assert-config",
          "proofTarget": "expo-config"
        },
        "assert-config": {
          "action": "assert_output",
          "description": "Confirm Expo config contains a slug field.",
          "source": "expo-config",
          "stream": "stdout",
          "contains": "\"slug\"",
          "next": "done",
          "proofTarget": "expo-config"
        },
        "done": {
          "action": "end",
          "status": "pass"
        }
      }
    }
  }
}
