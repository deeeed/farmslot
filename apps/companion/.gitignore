node_modules/
.expo/
dist/
build/
*.tsbuildinfo
.env.local
.env.*.local
scripts/agentic/agentic.local.conf

# npm — this project uses yarn v4, never npm
package-lock.json

# Native directories are generated by Expo CNG/EAS, not source of truth.
android
ios

# Yarn 4 (Berry) — keep releases/plugins/sdks, ignore everything else
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*

# Store screenshot generator intermediates
store-assets/generated-svg/
