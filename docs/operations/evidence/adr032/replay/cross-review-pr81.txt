ADR-032 split verifiers — round-2 re-review (chore/adr032-split-verifiers @ bcfb11b)
Reviewed: 2026-06-27
Prior review: cross-review-replay-pr.txt (REQUEST_CHANGES on blocking 1–4)

VERDICT: APPROVE

---

Blocking fix verification (items 1–4)

1) Cross-review regex anchored — RESOLVED
   File: scripts/verify-adr032-merge-process.sh:34-40,94-96
   - Line-scoped `crossReviewApproves()` checks `^VERDICT:\s*APPROVE pending CI\s*$` then
     `^VERDICT:\s*APPROVE\s*$`; any other `^VERDICT:` line rejects.
   - Regression: APPROVE_WITH_CHANGES, REQUEST_CHANGES, "do not APPROVE pending CI",
     and trailing junk on verdict line now fail (node harness run in review).

2) headRefOid-scoped approval — RESOLVED
   Files: scripts/lib/adr032-pr-chain-validate.mjs:13-20
          scripts/capture-pre-merge-evidence.sh:42-53,64
          scripts/verify-adr032-merge-process.sh:66-70
   - `independentApproveOnHead()` requires `r.state === 'APPROVED'`, author !== PR author,
     and `r.commit?.oid === pr.headRefOid`.
   - Capture sets `hasIndependentApprove` from that helper; merge verifier re-validates on
     `pre.prView` (not trusting flag alone beyond initial gate).

3) Assert checks all three replay artifacts — RESOLVED
   File: scripts/assert-adr032-goal-evidence.sh:13-14
   - Gate now requires pr81-premerge-capture.json, pr81-postmerge.json, and cross-review-pr81.txt
     before invoking verify-adr032-merge-process.sh.
   - docs/operations/evidence/adr032/replay/README.md:9-13 documents all three files.

4) parseIsoMs validation — RESOLVED
   Files: scripts/verify-adr032-merge-process.sh:48-51,74-77,84-87
          scripts/lib/adr032-pr-chain-validate.mjs:7-10
   - capturedAt, mergedAt, and approve.submittedAt validated via parseIsoMs; distinct error
     messages for invalid timestamps before ordering checks.

---

Additional notes (non-blocking)

- scripts/capture-pre-merge-evidence.sh:38-87 — node block runs from repo root via subshell;
  imports shared lib (requiredChecksGreen) — reduces drift vs prior inline duplicate.
- scripts/capture-pre-merge-evidence.sh:73 — still writes JSON before validation exit (unchanged;
  acceptable if documented as debug artifact).
- Replay directory has README only (no frozen JSON yet); merge-process verifier not executed
  end-to-end in this review.
- scripts/verify-adr032-shipped-main.sh unchanged; full assert-adr032-goal-evidence.sh not run
  here (e2e/gate/typecheck are CI/heavy-local).

---

Recommendation

Round-2 blocking items 1–4: satisfied at HEAD.
APPROVE pending CI: run assert-adr032-goal-evidence.sh (or CI job that invokes it) on this
branch; after replay PR lands, run verify-adr032-merge-process.sh against frozen replay trio.
