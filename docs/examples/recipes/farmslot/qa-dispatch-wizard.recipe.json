{
  "$schema": "https://farmslot.io/schemas/recipe-v1.schema.json",
  "title": "General dispatch Review and farm QA wizard",
  "description": "Real browser controls and an isolated production gateway prove QA profile/default/input selection, invalid JSON refusal, distinct queue fields, workspace-only farms, legacy links and existing dev controls. Orchestration remains disabled.",
  "workflow": {
    "entry": "wizard-interface",
    "nodes": {
      "wizard-interface": {
        "action": "command",
        "cmd": "node --import tsx scripts/e2e-qa-dispatch-wizard.mts",
        "timeout_ms": 240000,
        "intent": "Drive actual wizard controls, inspect stored queue requests via gateway RPC and retain screenshots while asserting no model runs launch.",
        "next": "done"
      },
      "done": { "action": "end", "status": "pass" }
    }
  }
}
