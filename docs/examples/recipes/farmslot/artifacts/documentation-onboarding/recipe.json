{
  "$schema": "https://farmslot.io/schemas/recipe-v1.schema.json",
  "title": "Farmslot recipe protocol documentation and onboarding smoke",
  "description": "Validates protocol examples, self-validation fixtures, and onboarding commands with the shared v1 contract validator.",
  "paramsSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "repo_root": {
        "type": "string",
        "default": "."
      },
      "command_center_dir": {
        "type": "string",
        "default": "apps/command-center"
      }
    }
  },
  "workflow": {
    "entry": "validate-backend-example",
    "nodes": {
      "validate-backend-example": {
        "action": "command",
        "cmd": "cd apps/command-center && yarn farmslot recipe validate ../../../docs/examples/recipes/backend-command-v1.recipe.json",
        "next": "validate-ui-example",
        "intent": "Validate the backend/API sample recipe with the shared Farmslot v1 contract validator."
      },
      "validate-ui-example": {
        "action": "command",
        "cmd": "cd apps/command-center && yarn farmslot recipe validate ../../../docs/examples/recipes/ui-live-v1.recipe.json",
        "next": "validate-self-fixtures",
        "intent": "Validate the UI live replay sample recipe with the shared Farmslot v1 contract validator."
      },
      "validate-self-fixtures": {
        "action": "command",
        "cmd": "cd apps/command-center && yarn test:protocol && yarn test:recipe-harness",
        "next": "index-artifacts",
        "intent": "Validate every Farmslot self-validation recipe package and ensure it emits summary, trace, manifest, and resolved recipe artifacts."
      },
      "index-artifacts": {
        "action": "index_artifacts",
        "next": "done",
        "artifacts": ["reports/contract-validation.json", "logs/onboarding-validation.log"],
        "intent": "Publish validator report, onboarding command log, summary, trace, and resolved recipe."
      },
      "done": {
        "action": "end",
        "status": "pass"
      }
    }
  }
}
