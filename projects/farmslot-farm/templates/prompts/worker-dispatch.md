Follow the checklist in {{TASK_FILE}}. After each step, run {{TASK_DIR}}/mark N before continuing.

Quality contracts (synced to slot runtime):
- Workers: read `{{recipe_quality_path}}` before recipe proof.
- Reviewers: read `{{review_quality_path}}` and apply fs-recipe-quality for evidence audit.