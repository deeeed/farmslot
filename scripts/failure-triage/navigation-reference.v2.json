{
  "version": 1,
  "status": "draft-unsealed",
  "references": [
    {
      "caseId": "device-deployment",
      "label": "environment",
      "requiredReadIds": ["e1"],
      "nextCheck": "Inspect the installer error and free simulator storage before retrying.",
      "family": "device-capacity",
      "rationale": "The installer reports ENOSPC while writing the application bundle; the build itself completed."
    },
    {
      "caseId": "checkout-revision",
      "label": "environment",
      "requiredReadIds": ["e4"],
      "nextCheck": "Inspect the assigned slot revision and fetch the missing object.",
      "family": "checkout-depth",
      "rationale": "The assigned slot reports that the selected revision is absent; preparation and dispatch events are consequences."
    },
    {
      "caseId": "profile-request",
      "label": "environment",
      "requiredReadIds": ["e2", "e3"],
      "nextCheck": "Compare the outgoing host with the selected profile host.",
      "family": "profile-host-selection",
      "rationale": "The trace and selected profile disagree on the request host; a 403 alone cannot establish why."
    },
    {
      "caseId": "remote-state-assertion",
      "label": "test_harness",
      "requiredReadIds": ["e2", "e3"],
      "nextCheck": "Compare the completion predicate with the pending target state.",
      "family": "receipt-predicate",
      "rationale": "The completion condition accepts a local acknowledgement while the target screen remains pending."
    },
    {
      "caseId": "native-package",
      "label": "dependencies",
      "requiredReadIds": ["e2"],
      "nextCheck": "Inspect the native binary architecture selected by the module loader.",
      "family": "native-binary-variant",
      "rationale": "The module loader identifies an architecture mismatch; setup completion is only context."
    },
    {
      "caseId": "account-cache-key",
      "label": "implementation",
      "requiredReadIds": ["e2"],
      "nextCheck": "Inspect whether the balance cache key includes the selected account.",
      "family": "account-key-isolation",
      "rationale": "The balance cache state omits account identity; UI and network observations corroborate the impact."
    },
    {
      "caseId": "remote-certificate",
      "label": "external_service",
      "requiredReadIds": ["e3"],
      "nextCheck": "Inspect the remote endpoint certificate and its expiry.",
      "family": "remote-certificate-expiry",
      "rationale": "The remote endpoint presented an expired certificate; configuration remained unchanged."
    },
    {
      "caseId": "export-count-gap",
      "label": "unclear",
      "requiredReadIds": ["e2", "e4"],
      "nextCheck": "Recover both query filters and source snapshot identifiers before assigning a cause.",
      "family": "export-query-provenance",
      "rationale": "Both exports completed with different counts, but missing filters and snapshot IDs prevent deciding whether the requests or results differed."
    }
  ]
}
