{
  "$schema": "https://farmslot.io/schemas/recipe-v1.schema.json",
  "title": "Structured assessment provider compatibility",
  "description": "Set a new ASSESSMENT_PROVIDER_PROOF_OUT and a free ASSESSMENT_PROVIDER_PROOF_PORT. Uses an owned gateway and simulated transport; no external inference.",
  "workflow": {
    "entry": "providers",
    "nodes": {
      "providers": {
        "action": "command",
        "cmd": "FARMSLOT_ASSESSMENT_VALIDATION=1 node scripts/assessment-validation/providers.mjs",
        "timeout_ms": 120000,
        "intent": "Verify native and regular-LLM answers through actual RPCs, feedback without probability distributions, native HTTP-error receipts and ordinary-LLM malformed/model/stream-tail/timeout failures, including a malformed first SSE frame, malformed and omitted token counts with retained received usage and no retries, missing-key skips, restart persistence and independent consumer qualification.",
        "next": "done"
      },
      "done": { "action": "end", "status": "pass" }
    }
  }
}
