# Bad fixture spec — violates every lint rule at least once

**Project:** `farmslot-farm`

## Problem

Fixture for spec-lint self-tests: seeded violations, one per rule.

## Deliverables

1. Nothing real.

## Non-goals

## Acceptance Criteria

- The feature works well and users are happy.
- Confirm behavior after merge in production.
- Validate the post-merge rollout dashboards.
- Keep it stable through a 7-day observation window.
- Ship dark until the operator enables the flag.
- Keep monitoring for regressions in the weekly report.
- Let it soak before flipping the default.
- Empty inline code proves nothing: ``.
- A bare marker also proves nothing: artifact:
