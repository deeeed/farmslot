# Changelog

All notable changes to `@farmslot/theme` are tracked here.

## Unreleased

- Add a shared deterministic project color mapping for consistent project badges across clients.
- feat: rename the branch-maintenance flow `merge-main` → `update-branch` (flow color mapping + abbreviation label `MRG` → `UPD`).
- Active-development baseline; add user-facing changes here before release or package publication.
