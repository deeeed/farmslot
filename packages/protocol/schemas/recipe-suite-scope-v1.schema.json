{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://farmslot.io/schemas/recipe-suite-scope-v1.schema.json",
  "title": "Farmslot Recipe Suite Scope v1",
  "description": "The immutable set of opaque cases a recipe suite must resolve.",
  "type": "object",
  "required": ["$schema", "suite_id", "cases"],
  "properties": {
    "$schema": {
      "const": "https://farmslot.io/schemas/recipe-suite-scope-v1.schema.json",
      "description": "Canonical Recipe Suite Scope v1 schema URL."
    },
    "suite_id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable identifier shared by the scope and its result."
    },
    "cases": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "description": "Complete case set frozen before any case executes.",
      "items": { "$ref": "#/$defs/case" }
    }
  },
  "additionalProperties": false,
  "$defs": {
    "case": {
      "type": "object",
      "required": ["id"],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Opaque case identifier unique within the suite."
        }
      },
      "additionalProperties": false
    }
  }
}
